%h1 Check out our products!

.row.product-list
  - @products.each do |product|
    %article.product
      %h2= product.name
      %p= product.description
      %strong{id: product.id}
        = product.convert_currency(current_currency,@currency_exchange_data)
        = current_currency

      = link_to 'Show more', product

